#---------------問題4------------------#

# def trapezoid_area(top, bottom, height):
#     try:

#         top, bottom, height = top, bottom, height = float(top), float(bottom), float(height)

#         if top > 0 and bottom > 0 and height > 0: # 全ての長さが正の場合
#             area = (top + bottom) * height * 0.5
#             return area

#         else:
#             raise Exception # exceptに飛ばす
#     except:
#         return False

#--------------------------------------#